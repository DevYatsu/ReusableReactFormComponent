import { TextArea } from "../@types/textarea";
declare const TextArea: ({ name, register, placeholder, required, rows, }: TextArea) => import("react/jsx-runtime").JSX.Element;
export default TextArea;
